
@{
    ViewData["Title"] = "Login";
}

    <div class="row justify-content-md-center align-items-center">
        <div class="col col-lg-2">

        </div>
        <div class="col-md-6 align-self-center" style="top:200px">
            <h3>Bayi Giriş</h3>
            @if (ViewBag.LoginResult != null)
            {
    <div class="p-3 mb-2 bg-danger text-white">@ViewBag.LoginResult</div>}
            @using (Html.BeginForm("Login", "Dealer", FormMethod.Post, new { }))
            {
    <div class="form-group">
        <input type="text" class="form-control" placeholder="Bayi ID" value="" name="DealerId" required>
    </div>
                    <div class="form-group">
                        <input type="password" class="form-control" placeholder="Şifre" value="" name="Password" required>
                    </div>
                                    <div class="form-group">
                                        <input type="submit" class="btn btn-primary" style="width:50%; float:right" value="Giriş">
                                    </div>}


        </div>

        <div class="col col-lg-2">

        </div>
    </div>
